from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Part of project to build mobile application scan and detect pages being flipped or not',
    author='Usama Zia',
    license='MIT',
)
